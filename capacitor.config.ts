import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'profit_roles_app',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
